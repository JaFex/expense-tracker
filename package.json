{
  "name": "root",
  "private": true,
  "devDependencies": {
    "husky": "^9.1.7",
    "lerna": "^8.2.2"
  },
  "scripts": {
    "lint:staged": "lerna run lint:staged",
    "test": "lerna run test",
    "prepare": "husky"
  },
  "workspaces": [
    "web/*",
    "backend/*"
  ]
}